# Optomizer V3.3.0
This Update features stability and new features!
 ## This update now uses the more stable Java 21!!
# Change Log
> Added a kill proccess button (only for MacOS and Linux)
> Removed the ability to resize window so that UI does not break!
> Fix a bug where occasionaly MacOS drop down box does not work.
> Removed the `--sort=-ni` from MacOS because UNIX does not support it is a part of LINUX
> Changed how the PID bar works now users no lohger deal with PID problems now the application is much more simpler to use witht the help of the commands `| sort | uniq` out for Linux and MacOS